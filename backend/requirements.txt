fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
SQLAlchemy>=2.0,<3
pydantic-settings>=2.6,<3
pwdlib[argon2]>=0.2,<1
python-jose[cryptography]>=3.4,<4
python-multipart>=0.0.20,<1
email-validator>=2.2,<3
google-genai>=1.30,<2
pypdf>=5,<7
python-docx>=1.0.0
# Pure-Python MySQL driver for the Vultr Managed Database (no system
# libmysqlclient needed, unlike mysqlclient -- simplest option for a
# hackathon dev machine).
PyMySQL>=1.1,<2# S3 client for Vultr Object Storage (backend/services/storage.py). Only
# exercised when the VULTR_* settings are filled in; the import is lazy so
# this stays installable-but-unused for local SQLite development.
boto3>=1.34,<2
