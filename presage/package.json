{
  "name": "hackrice-presage-capture",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SmartSpectra (Presage) camera capture for one study round: stress, drowsiness, and the break recommendation.",
  "scripts": {
    "session": "node --env-file=../.env session.mjs"
  },
  "dependencies": {
    "@smartspectra/node-sdk": "3.3.0"
  }
}
