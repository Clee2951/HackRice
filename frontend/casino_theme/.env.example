# Where the FastAPI backend is reachable from the browser / Electron window.
# Must also appear in backend/core/config.py's CORS_ORIGINS (localhost:3000
# and 127.0.0.1:3000 are there by default).
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
